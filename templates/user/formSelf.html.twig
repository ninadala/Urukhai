{% extends "base.html.twig" %}

{% block stylesheets %}
    {{ parent() }}
        <link rel="stylesheet" href="{{ asset('style/formsPages.css')}}">
{% endblock %}

{% block body %}
<div class="d-flex mt-5">
    {{ form_start(user_form, {'attr': {'class': 'container'}})}}
    {{ form_row(user_form.username, { 'attr':{'class':'d-none'}})}}
    {{ form_row(user_form.lastname, { 'attr':{'class':'d-none'}})}}
    {{ form_row(user_form.firstname, { 'attr':{'class':'d-none'}})}}
    {{ form_label(user_form.password)}}
    <div class="input-group">
            {{form_widget(user_form.password)}}
            <span role="button" class="input-group-text" onClick="togglePassword()">
                    <i class="bi bi-eye" id="eye"></i>
                    <i class="bi bi-eye-slash d-none" id="eye-slash"></i>
            </span>
    </div>
    {{ form_errors(user_form.password)}}
    {{ form_row(user_form.email, { 'attr':{'class':'d-none'}})}}
    {{ form_row(user_form.roles, { 'attr':{'class':'d-none'}})}}
    {{ form_rest(user_form)}}
    <input class="btn btn-success mt-3" type="submit" value="Ok" id="btnSubmit">
</div>
{% endblock %}